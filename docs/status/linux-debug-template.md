# Linux 実機デバッグメモ

このテンプレートは、Linux 側でビルドや実行をしたときに
エラーや想定外の挙動が出た場合の記録用です。

使い方:

1. このファイルをコピーする
2. ファイル名を日付つきにする
3. 各項目を埋める
4. 必要なら GitHub に push する

ファイル名の例:

```text
docs/status/linux-debug-2026-04-14.md
```

---

## 1. 概要

- 日付:
- 担当:
- 対象ブランチ:
- 対象コミット:

## 2. 実行したコマンド

```bash
# ここに実行したコマンドを貼る
# 例
cmake -S . -B build
cmake --build build
./build/your_app
```

## 3. 期待結果

- ここに期待していた動作を書く

## 4. 実際結果

- ここに実際に起きたことを書く

## 5. エラーログ全文

```text
ここにエラーログ全文を貼る
```

## 6. git status

実行コマンド:

```bash
git status
```

出力:

```text
ここに出力を貼る
```

## 7. git diff

実行コマンド:

```bash
git diff
```

出力:

```diff
ここに出力を貼る
```

## 8. 再現手順

1. ここに手順を書く
2. ここに手順を書く
3. ここに手順を書く

## 9. 補足

- 設定ファイルを変えたか
- どの実機を使ったか
- センサや通信の状態
- 再現率
- 一時的に直った条件

## 10. Linux 側で修正した場合

修正したファイル:

- ここに書く

修正内容:

- ここに書く

実行したコマンド:

```bash
git add .
git commit -m "describe linux debug fix"
git push
```

## 11. Windows 側に渡すとき

Windows 側では次を実行する。

```powershell
git pull
```

そのあと、このメモを Codex に見せれば調査を続けやすい。
